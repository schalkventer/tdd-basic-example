/**
 * @typedef {object} Task
 * @prop {string} id
 * @prop {string} title
 * @prop {Date} created
 * @prop {'completed' | 'pending' | 'archived'} status
 */

export const Task = () => {
  return <div>123</div>;
};
